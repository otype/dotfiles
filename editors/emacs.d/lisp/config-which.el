;;; package --- which-key Plugin configuration
;;; Commentary:
;;; This includes all configurations for the 'which-key' plugin

;;; Code:

(which-key-mode t)
(setq which-key-popup-type 'minibuffer)

(provide 'config-which)
;;; config-which.el ends here
